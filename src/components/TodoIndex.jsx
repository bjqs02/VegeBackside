import React, { Component } from 'react';
// import axios from 'axios';  

        class TodoIndex extends Component {
            state = {  }
            render() { 
                return ( 
                    <div>It's work. (todo index) </div>
                );
            }
        }
        
        export default TodoIndex;